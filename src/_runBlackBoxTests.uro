force copy 'bin/Release/Uroboros.exe'
  to 'test/blackbox'
  as 'perun2.exe';
  
inside 'test/blackbox' {
  if exists('perun2.exe') {
    open 'run.py';
    if not success {
      error
    }
  }
  else {
    error
  }
}