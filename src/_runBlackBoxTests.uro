force copy 'bin/Release/Uroboros.exe'
  to 'test/blackbox'
  as 'uro.exe';
  
inside 'test/blackbox' {
  if exists('uro.exe') {
    open 'run.py';
    if not success {
      error
    }
  }
  else {
    error
  }
}